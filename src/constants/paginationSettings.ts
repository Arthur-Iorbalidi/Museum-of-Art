const paginationGaps = [-2, -1, 0, 1, 2];

const arrowsGaps = {
  back: -1,
  forward: 1,
};

const firstPageNumber = 1;
const maxPageNumber = 10000;

export { arrowsGaps, firstPageNumber, maxPageNumber, paginationGaps };
